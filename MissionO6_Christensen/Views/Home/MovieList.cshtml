@model IEnumerable<MissionO6_Christensen.Models.Movie>

<h2>Movie Collection</h2>
<table class="table">
    <thead>
    <tr>
        <th>Title</th>
        <th>Category</th>
        <th>Director</th>
        <th>Year</th>
        <th>Rating</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var movie in Model)
    {
        <tr>
            <td>@movie.Title</td>
            <td>@movie.Category</td>
            <td>@movie.Director</td>
            <td>@movie.Year</td>
            <td>@movie.Rating</td>
        </tr>
    }
    </tbody>
</table>

<a asp-action="EnterMovie" class="btn btn-primary">Add a Movie</a>